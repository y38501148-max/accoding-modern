{
  "name": "accoding-modern",
  "version": "1.19.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build && node --check accoding-modern.user.js && npm test"
  },
  "engines": {
    "node": ">=18"
  }
}
