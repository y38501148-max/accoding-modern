# 页内代码复核 1.10.0

班级页使用 `/oj-review-api/v2/reviews/search`，携带当前班级已匹配的 OJ 用户 ID，由服务器按比赛、成员、题目和标签筛选，每页 30 条。打开本页无需下载全场提交元数据。

列表只显示模型明确输出 `ai_suspected=true` 的结果。详情提供当前源码、对应原文行及前次提交链接；前次源码由 OJ 原站提供，不复制到 server3。人工记录继续保存在当前浏览器，可与筛选结果一并导出。

server3 保存疑似源码与必要证据，并保存不含源码的批次进度。完整源码、正常结果和队列移至 Mac / ROG E 盘。负例更新只传递删除标识，推理失败保持失败状态。浏览器与只读令牌不能创建或恢复任务，任务仍由 Mac 签名授权。

第二版模型尚未执行时，旧版的普通/优先级记录不会冒充第二版结果。暂停状态单独显示，不能将空列表理解为所有代码已通过复核。
